# Auto Face Tracker using OpenCV and Arduino

## 📌 Project Overview
This project uses **OpenCV (Python)** and an **Arduino-controlled stepper motor** to build an **Auto Face Tracking Camera System**.  
The system detects a face using your **PC camera** and rotates the camera using a stepper motor to keep the face centered.

...
